import { FilnameTruncatorPipe } from './filname-truncator.pipe';

describe('FilnameTruncatorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilnameTruncatorPipe();
    expect(pipe).toBeTruthy();
  });
});
